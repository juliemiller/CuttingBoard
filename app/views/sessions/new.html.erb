<div class="signInPage">
<div class="blur">
</div>
</div>
	<div class="signInForm">
		<%= render "form", user: @user, action_url: session_url, submit_button: "Log In" %>
		<p>
		New user? <%= button_to("Sign Up", new_user_url, method: :get) %>
		</p>

		<%= render "users/demo" %>
	</div>
